<?php



    // 150_comparePassword.iniにはパスワードがあるかどうか確認する。
    if ( isset($ini['compare']) && isset($ini['compare']['password']) && count($ini['compare']['password']) ) {
        $output['compare']['string_en'] = implode(',', $ini['compare']['password']);
    }

?>