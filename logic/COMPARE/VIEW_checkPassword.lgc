<?php

    /* -------------------------------------
    ビュー
    ------------------------------------- */
    $retJSON = $output['compare_checkPassword'];


    // ----------------------------------------------------------------
    // JSONの出力
    header("content-type: application/x-javascript; charset=utf-8");
    die(json_encode($retJSON));
    exit;

?>
